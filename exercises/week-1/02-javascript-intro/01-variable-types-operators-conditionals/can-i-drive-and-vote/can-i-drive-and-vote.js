/**
 * canIDriveAndVote - A function that checks if a person can legally drive and vote based on their age.
 *
 * @param {number} age - The age of the person
 * @returns {boolean} - True if the person is 18 years old or older, false otherwise
 *
 * Usage:
 * const result = canIDriveAndVote(19);
 * // Output: true
 */
function canIDriveAndVote(age) {
    
}

module.exports = canIDriveAndVote;
