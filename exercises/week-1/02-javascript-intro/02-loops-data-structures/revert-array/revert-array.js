/**
 * revertArray - A function that reverses the elements in a given array without using Array.reverse.
 *
 * @param {Array} arr - The input array
 * @returns {Array} - A new array with the elements in reverse order
 *
 * Usage:
 * const result = revertArray([1, 2, 3]);
 *  // Output: [3, 2, 1]
 */

function revertArray() {
}

module.exports = revertArray;
